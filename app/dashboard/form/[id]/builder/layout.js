export const metadata = {
    title: 'Form Builder | Anuminow Solution ERP',
    description: 'Edit form fields and document layout',
    robots: 'index, follow',
}

export default function FormBuilderLayout({ children }) {
    return children
}
