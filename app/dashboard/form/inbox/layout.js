export const metadata = {
    title: 'Form Inbox | Anuminow Solution ERP',
    description: 'Review and track form approval workflow items',
    robots: 'index, follow',
}

export default function FormInboxLayout({ children }) {
    return children
}
