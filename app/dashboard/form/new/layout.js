export const metadata = {
    title: 'Create Form | Anuminow Solution ERP',
    description: 'Create a reusable document form template',
    robots: 'index, follow',
}

export default function CreateFormLayout({ children }) {
    return children
}
