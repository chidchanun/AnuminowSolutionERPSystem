export const metadata = {
    title: 'Permission Matrix | Anuminow Solution ERP',
    description: 'จัดการสิทธิ์การเข้าถึงของแต่ละ permission role',
    robots: 'index, follow',
}

export default function PermissionLayout({ children }) {
    return children
}
