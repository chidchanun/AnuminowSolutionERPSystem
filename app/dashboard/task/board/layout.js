export const metadata = {
    title: 'Kanban board Task | Anuminow Solution ERP',
    description: 'ระบบแสดง board งาน',
    robots: 'index, follow',
}

export default function TaskListLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}