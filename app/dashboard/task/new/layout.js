export const metadata = {
    title: "Create Task | Anuminow ERP System",
    description: "ระบบเพิ่มงาน และมอบหมายงานให้กับพนักงาน",
    robots: "index, follow"
}

export default function CreateTaskLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}