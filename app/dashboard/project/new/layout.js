export const metadata = {
    title: 'เพิ่มโปรเจ็กใหม่ | Anuminow Solution ERP',
    description: 'หน้าฟอร์มเพิ่มโปรเจ็กใหม่ในระบบ ERP',
    robots: 'index, follow',
}

export default function CreateProjectLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}